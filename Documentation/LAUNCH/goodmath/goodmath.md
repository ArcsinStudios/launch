# `goodmath`

## Constants
<small>Defined in `goodmath_consts.h`</small>
|Constant|Value|Description|
|---|---|---|
|`PI`|`3.14159'26535'89793`|pi|
|`E`|`2.71828'18284'59045`|Euler's number (natural constant)|
|`PHI`|`1.61803'39887'49894`|golden ratio|

## Functions
<small>Defined in `goodmath_impl.h`</small>
|Function|Description|
|---|---|
|`abs`|same as `std::abs`|
|`floor`|same as `std::floor`|
|`ceil`|same as `std::ceil`|
|`round`|same as `std::round`|
|`trunc`|same as `std::trunc`|
|`floor_ll`|same as `std::floor`, returns `long long`|
|`ceil_ll`|same as `std::ceil`, returns `long long`|
|`round_ll`|same as `std::round`, returns `long long`|
|`trunc_ll`|same as `std::trunc`, returns `long long`|
|`max`|same as `std::max`|
|`min`|same as `std::min`|
|`pow`|same as `std::pow`|
|`mod`|same as `std::fmod`|
|`gcd`|same as `std::gcd`|
|`lcm`|same as `std::lcm`|
|`sqrt`|same as `std::sqrt`|
|`cbrt`|same as `std::cbrt`|
|`nrt`|the nth root|
|`exp`|same as `std::exp`|
|`ln`|same as `std::log`|
|`lg`|same as `std::log10`|
|`log2`|same as `std::log2`|
|`log`|logarithm with any base|
|`hypot`|same as `std::hypot`|
|`dtor`|converts degrees to radians|
|`rtod`|converts radians to degrees|
|`sin`|same as `std::sin`|
|`cos`|same as `std::cos`|
|`tan`|same as `std::tan`|
|`arcsin`|same as `std::asin`|
|`arccos`|same as `std::acos`|
|`arctan`|same as `std::atan`|
|`sinh`|same as `std::sinh`|
|`cosh`|same as `std::cosh`|
|`tanh`|same as `std::tanh`|
|`arsinh`|same as `std::asinh`|
|`arcosh`|same as `std::acosh`|
|`artanh`|same as `std::atanh`|